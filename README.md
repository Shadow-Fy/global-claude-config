# global-claude-config

[Claude Code](https://claude.ai/code) 全局配置。

## 内容

- `CLAUDE.md` — 全局行为指令
- `commands/` — 自定义命令
- `skills/` — 自定义技能
- `plugins.json` — 推荐插件清单

## 使用

在 Claude Code 中执行 `/plugin install <name>@<marketplace>`，具体插件见 `plugins.json`。
